# Pinned versions for Colab training runs -- see docs/colab.md.
#
# WHY PINNED: Colab's preinstalled torch/numpy/etc. drift over time and can
# silently change numerical behavior (kernel selection, cudnn algorithms,
# even normalization edge cases) versus what produced the 0.1057 baseline
# locally. A version mismatch against these exact versions invalidates any
# direct comparison to that baseline -- if Colab can't satisfy a pin
# (e.g. an unsupported CUDA/driver combination), note the substitution
# explicitly in that run's run_manifest.json / LOG.md entry rather than
# silently training on different versions.
#
# Install torch/torchvision FIRST with the matching CUDA wheel, exactly
# like the local setup (see requirements.txt's header comment):
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
# then:
#   pip install -r requirements-colab.txt

rasterio==1.4.4
numpy==2.2.6
scipy==1.15.3
opencv-python==5.0.0
scikit-image==0.25.2
matplotlib==3.10.9
requests==2.34.2
py7zr==1.1.3
segmentation-models-pytorch==0.5.0
timm==1.0.28
pyyaml==6.0.3
pytest==9.1.1
