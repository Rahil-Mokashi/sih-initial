venv/
__pycache__/
*.pyc
.env
.claude/

# raw/processed data is large and reproducible from the scripts in /scripts
data/raw/
data/processed/
*_stdout.log
*_stderr.log

# checkpoints/ holds protected reference checkpoints (see Phase 0/1 docs) --
# the binaries are large and belong on Drive/local disk, not git; MANIFEST.json
# (sha256 + config) is the part that's actually meant to be versioned.
checkpoints/**/*.pt
